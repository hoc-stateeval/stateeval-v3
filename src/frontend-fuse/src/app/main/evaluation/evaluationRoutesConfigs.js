import ArtifactsRoutesConfig from './ArtifactsRoutesConfig';
import DashboardsRoutesConfig from './DashboardsRoutesConfigs';
import SetupRoutesConfig from './SetupRoutesConfig';

const evaluationRoutesConfigs = [ArtifactsRoutesConfig, DashboardsRoutesConfig, SetupRoutesConfig];

export default evaluationRoutesConfigs;
